import { Card } from '@/components/ui/primitives';

// Replaced in Phase 2 by the full Overview (health strip, price, movers, TVL, activity).
export default function OverviewPage() {
  return (
    <Card className="p-6">
      <h1 className="text-xl font-bold">Cookie Pulse</h1>
      <p className="mt-1 text-sm text-muted">Overview lands in Phase 2.</p>
    </Card>
  );
}
